#/usr/bin/env bash
# This Bash script stops 4-to_infinity_and_beyond process

#get the pid of the prcess first
id=$(grep "bash ./4-to_infinity_and_beyond" -f)
#kil the process
if [ -n "$id"]
then
    kill -l5 "id"
fi